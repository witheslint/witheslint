interface Schema0 {
/**
 * if the innerHTML value is guaranteed to be a static HTML string (i.e. no user input), allow it
 */
  allowStatic?: boolean
}

export type NoInnerhtmlRuleOptions = [Schema0]
