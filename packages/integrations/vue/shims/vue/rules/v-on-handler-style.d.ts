type Schema0 = (('inline' | 'inline-function') | ['method', ('inline' | 'inline-function')])

interface Schema1 {
  ignoreIncludesComment?: boolean
}

export type VOnHandlerStyleRuleOptions = [Schema0, Schema1]
